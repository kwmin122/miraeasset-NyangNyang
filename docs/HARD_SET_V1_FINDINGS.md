# 고난도 10문(hard_v1) 실측 — 선우 전달용

- **작성**: 경욱 / 2026-09-02
- **파일**: `evalset/questions_hard_v1.jsonl` (10문, 유형5 8 + 유형6 2)
- **실행**: 배포 서버 `http://49.50.143.160` (`/health` → `agent_mode: sunwoo`), 2026-09-02 17:58
- **원 결과**: `evalset/results_20260902_175836.jsonl` (gitignore 대상 — 로컬에만 있다. 아래 명령으로 재현)
- **재현**: `python3 evalset/run_eval.py --base http://49.50.143.160 --file evalset/questions_hard_v1.jsonl`

## 요약

**4 / 10.** 기존 dev 58문은 그대로 **58/58**이다 — `questions_v1.jsonl`은 손대지 않았다.
hard_v1은 "지금 파이프라인이 어디서 깨지는지"만 보려고 따로 만든 셋이고, 제출 성적과 무관하다.

골드는 10문 전량 `data/corpus/` 원문 XML에서 직접 확인한 값만 썼다(CLAUDE.md 절대규칙 6).
최대 지연 18.2초 — 성능 문제는 없다.

| qid | 결과 | ev | 노린 것 |
|---|---|---|---|
| T5-C-005 | ❌ | 0.5 | 현대건설 아미랄 14단 정정체인 **양 끝단 금액** |
| T5-C-006 | ✅ | 1.0 | 같은 체인 **양 끝단 계약상대**(ARAMCO→SATORP) |
| T5-C-007 | ✅ | 1.0 | 삼성전자 상장폐지 결정 vs 실행 **주식수** |
| T5-C-008 | ❌ | 1.0 | 삼성전자 **실제** 매매거래종료일 (폐지예정일이 함정) |
| T5-C-009 | ✅ | 1.0 | OCI홀딩스 잔여금액 승계 |
| T5-C-010 | ❌ | 0.0 | 교차기업 + 합산: SK하이닉스 ← 한미반도체 |
| T5-C-011 | ❌ | 0.0 | 교차기업: HMM ← 한화오션 |
| T5-C-012 | ✅ | 1.0 | 대우건설 집행정지 결과 **부재** |
| T6-O-010 | ❌ | 0.0 | 교차기업 다건: KAI ← 한화에어로 |
| T6-O-011 | ❌ | 0.0 | 교차기업 다건: LIG넥스원 ← 한화에어로 |

유형5 4/8, 유형6 0/2.

**구멍 5는 이 10문 밖에서 나왔다.** 모호 질의를 따로 찔러보다 발견했고, hard_v1 에는 안 걸린다.
재현 3/3 이라 같은 문서에 붙여둔다.

---

## 구멍 1 — 교차기업 4문 전멸 (ev 0.00 × 4)  ★구조적

"A사가 B사와 맺은 계약"에서 **공시 주체가 B사**인 경우를 하나도 못 찾는다.

`collect`가 manifest의 `corp_name`으로 문서를 거른다. 그런데 질문의 주어가 A(발주·수요 측)일 때
그 계약 공시는 **B의 이름으로 접수**돼 있고, A라는 이름은 B 공시의 **본문 안에만** 있다.
계획 단계에서 플래너가 볼 수 있는 건 질문 문자열뿐이니 B를 지목할 근거가 없다.

실제로 4문 모두 플래너가 **질문의 주어 회사만** 뒤졌다:

| qid | 플래너가 뒤진 곳 | 정답이 있는 곳 |
|---|---|---|
| T5-C-010 | SK하이닉스 사업보고서·반기보고서 | 한미반도체 단일판매·공급계약 (2024년 3건) |
| T5-C-011 | HMM 단일판매·공급계약 | 한화오션 단일판매·공급계약 |
| T6-O-011 | LIG넥스원 대량보유상황보고서 | 한화에어로스페이스 단일판매·공급계약 (2024-12-24 2건) |
| T6-O-010 | 한화에어로 계약 다수(도달은 함) | 그중 KAI 건 3건만 골라야 하는데 못 고름 |

T6-O-010만 예외적으로 한화에어로 문서에 도달했다. 하지만 **계약상대로 거르지 못해**
합병결정·호주 레드백·Vertical Aerospace까지 섞어 나열했다. 즉 도달해도 본문의 계약상대
필드로 좁히는 수단이 없다.

### 그중 2건은 "모른다"가 아니라 **자신 있게 틀린 답**을 냈다

심사 관점에서 이게 제일 위험하다. 근거 접수번호까지 달고 나온다.

**T5-C-011** — 질문: "HMM이 2025년 한화오션에 발주한 선박 건조 계약금액은?"
답변은 HMM이 **Vale International SA에 제공하는 운송계약** 2건(636,164,150,160원 /
430,341,938,400원)을 발주 건인 것처럼 제시했다. 방향이 정반대다(HMM=공급자).
근거: `20250527800522`, `20250912800061`. 정답은 1,070,700,000,000원.

**T6-O-010** — 질문: "KAI가 한화에어로에서 공급받기로 한 계약은 몇 건, 각 금액은?"
답변 첫 항목이 **한화에어로↔한화방산 회사합병 결정 공시**(`20230109000175`)다.
KAI와 무관하다. 정답 3건(370,007,842,050 / 473,098,793,200 / 472,836,888,000)은 하나도 없다.

### 참고: 이건 우리 코퍼스에서 드물지 않다

계약류 공시 본문을 전수 훑어 코퍼스 70개 기업명(별칭 포함)과 교차해 보니 **약 60건**이 나온다.
최다는 한미반도체→SK하이닉스 9건. 방어 여부는 선우 판단이다.

---

## 구멍 2 — 긴 정정 체인 top-k 절단 (T5-C-005, ev 0.5)

아미랄 PKG#1,4는 최초 공시 1건 + 정정 14건 = **15개 문서** 체인이다.
답변은 **12건만** 인용하고 끊겼다(최초 + 11차 정정). 빠진 3건은
`20241128800562`, `20250929800639`, `20260120800597`.

그래서 최종 계약금액 **3,090,076,756,644원**(`20260120800597`)이 답에 없고,
11차 값 3,072,243,348,055원이 사실상 "최종"처럼 제시됐다.
최초 금액 6,554,463,847,260원은 맞혔다 — 체인 추적 자체는 되는데 **길이에서 잘린다.**

골드 확인: `correction_map.json` 에서 `20260120800597` 은 `superseded_by` 가 없는 **종단**이고,
3,090,076,756,644원은 그 문서와 2025 사업보고서·2026 1분기보고서에 모두 실재한다.

---

## 구멍 3 — 금액 보강 각주가 **엉뚱한 문서의 정정 전** 값을 집는다 (`plan.py:1003 _postfix`)  ★제출 셋에도 있음

이게 이번에 나온 것 중 제일 고치기 쉽고, **유일하게 제출 셋(58문)에도 이미 들어가 있는** 문제다.

`_postfix`의 금액 보강은 근거 뭉치를 flatten한 뒤
`계약금액[^\n]{0,14}?(금액)` 으로 첫 미매칭 값을 뽑아 각주로 붙인다.
그런데 정정공시 본문은 이렇게 생겼다:

```
정정항목 | 정정전 | 정정후 | 2. 계약내역 중 계약금액(원) | 154,102,712,000 | 178,800,382,017
```

`계약금액` 바로 뒤 14자 안에 오는 건 **정정 전** 칸이다. 그래서 규칙적으로 옛 값을 집는다.
`CORRECTION_RULES`에 "'정 정 후' 값만 쓰라"를 넣어 HCX를 잡아놨는데,
그 뒤에 붙는 코드 보강이 정정 전 값을 도로 끌고 들어온다.

원문 대조로 확인한 3건 (전부 `정정전` 칸):

| 문항 | 각주가 붙인 값 | 실제 정체 | 출처 |
|---|---|---|---|
| T5-C-005 | 3,077,703,837,261 | 6차 정정의 **정정전** (정정후는 3,083,318,360,998) | `20250331802494` |
| T6-O-010 | 154,102,712,000 | **정정전** (정정후는 178,800,382,017) | `20230410800882` |
| **TR-NAME-001 (dev, 통과)** | 35,395,538,396 / 35,423,244,432 | 무관한 계약의 **정정전/정정후 쌍** | `20241111800001` |

TR-NAME-001이 특히 나쁘다. 답변은 인도네시아 경찰 계약 **198,437,328,900원**을 맞게 말해놓고,
바로 아래 "※ 근거 원문에 적힌 금액"으로 **전혀 다른 계약**의 354억/354억을 붙였다.
저 두 숫자는 답변이 인용한 근거(`20250711800172`)에 **없다** — 회수된 12건 중 다른 문서에서 왔다.

**여기서 결함이 둘이라는 게 드러난다.** 하나는 위에서 말한 칸 오인(정정전을 집음)이고,
다른 하나는 **문서 오인**이다. TR-NAME-001은 칸을 틀린 게 아니라 **문서를 틀렸다** —
정정전·정정후 **둘 다** 집었고 둘 다 무관한 계약의 값이다.
그래서 "정정후 칸을 집게 고친다"만으로는 TR-NAME-001이 **안 고쳐진다**. 여전히 35,423,244,432를,
여전히 남의 계약에서 인용한다.

뿌리는 `plan.py:1044` 다:

```python
full = context + " ".join((it.get("text") or "")
                          for r in results.values() for it in (r.get("items") or []))
```

회수된 모든 문서의 본문을 **한 덩어리로 이어붙인 뒤** 정규식을 돌린다.
그래서 매치가 어느 문서에서 나왔는지 알 방법이 코드에 없다.
칸 오인은 이 위에서 벌어지는 별개 증상이고, 문서 오인이 셋 다를 설명한다.

**제출 셋 영향**: dev 58문 중 **13문**의 답변에 이 각주가 붙어 있다
(T1-C-003, TR-NAME-001, T1-C-004, T3-C-002, T3-C-005, T5-C-003, T6-O-005, T4-O-010,
TR-COR-002, T4-O-013, T6-O-007, T4-O-016, T6-O-008).
**13문 다 채점은 통과한다** — accept_patterns가 본문에서 이미 맞아서 각주는 점수에 안 잡힌다.
하지만 사람이 읽으면 "근거 원문"이라고 명시된 숫자가 답과 다르다. 정성 평가에서 손해다.

고칠 방향(선우 판단). **위가 아래를 포함한다** — 위 두 개는 검증한 3건을 다 덮고,
맨 아래는 T5-C-005·T6-O-010만 덮고 TR-NAME-001은 못 덮는다:

- **문서 단위로 훑기** — `full` 로 뭉뚱그리지 말고 item 하나씩 정규식을 돌려,
  답변이 인용한 접수번호에서 나온 매치만 각주에 쓴다. 셋 다 고쳐진다.
- **근거가 2건 이상이면 아예 발화하지 않기** — 제일 작고 제일 안전하다.
  단건 회수(정정 없는 단순 질의)에서만 각주가 붙으므로 오인 자체가 불가능해진다.
  dev 13문 중 각주가 사라지는 문항이 생기지만, 13문 다 본문에서 이미 채점을 통과하므로 점수 영향 없음.
- (위 둘 중 하나를 한 뒤 덤으로) `정정전 | 정정후` 표에서 **뒤쪽** 값 집기 —
  이것만 단독으로 하면 TR-NAME-001은 그대로 남는다.

## 구멍 4 — 검색은 성공, 판정에서 실패 (T5-C-008, ev **1.00**)

질문: "삼성전자 룩셈부르크 DR 우선주는 **실제로** 언제 매매거래가 종료되었는가?"

파이프라인은 **결정 공시와 실행 공시를 둘 다 회수했다**(근거회수 1.00):

- `20241031000508` 결정 — 폐지(예정) 일자 **2025년 3월 31일**, 기타주식 38,685,850주
- `20250213001808` 실행 — 매매 거래 종료일 **2025년 2월 13일**, 기타주식 39,191,100주

그런데 둘을 **날짜순으로 나란히 나열하고 끝냈다.** 어느 쪽이 "실제로 일어난 일"인지 고르지
않았다. must_not(2025년 3월 31일)에 걸려 오답.

검색 문제가 아니라 **HCX#2 합성 측** 문제다. 앞서 나온 귀속 이슈와 같은 계열로 보인다.
"예정/결정 vs 실행/확정"이 한 문맥에 같이 들어오면 실행본을 사실로 삼는 규칙이
`CORRECTION_RULES` 옆에 하나 필요해 보인다.

---

## 구멍 5 — 계획기가 `topic` 을 빠뜨리면 **조용히 기권**한다 (`plan.py PLAN_SYSTEM`)  ★hard_v1 밖에서 발견

hard_v1 10문에는 안 걸렸다. 모호 질의를 따로 찔러보다 나왔고 **3/3 재현**된다.
같은 질문을 두 가지로 바꿔 쓰면 계획 JSON이 갈린다. **recency 는 둘 다 붙는다. 갈리는 건 `topic` 하나다.**

```
✅ "현대건설이 가장 최근에 공시한 계약의 계약금액은 얼마인가?"
   계획 {"tool":"collect","corp":"현대건설","recency":"latest","topic":"공급계약 체결"}
   → 제목 키워드 92건 → 가장 최근 1건 (20260330) → 2,748,850,000,000원

❌ "현대건설이 최근에 공시한 계약의 금액은?"
   계획 {"tool":"collect","corp":"현대건설","recency":"latest"}          ← topic 없음
   → 최신본 58건 → 가장 최근 1건 (20260515 **분기보고서**) → "확인할 수 없습니다"
```

**여기서 `collect` 와 `_abstain` 은 죄가 없다.** topic 이 없으면 "그 회사의 가장 최근 공시"는
정말로 분기보고서이고, 분기보고서에는 계약금액 칸이 없으니 기권이 맞다.
**결함은 상류 — 계획기가 topic 을 뽑지 못한 것**이다. `plan.py:248 _RECENCY_WORDS` 에
"최근"이 이미 들어 있어 recency 는 두 경우 다 정상으로 붙는다.

### 재현 3건 — 공시 유형을 일상어로 부르면 빠진다

| 질문 | 계획의 topic | 결과 |
|---|---|---|
| 삼성전자가 최근에 한 **자사주 관련 결정** 알려줘 | `자기주식 취득·처분 결정` | ✅ 정답 |
| 현대건설 최근 **수주 계약** 얼마야? | `''` (빈 문자열) | ❌ "찾을 수 없습니다" |
| SK하이닉스 최근 **계약** 규모는? | (키 없음) | ⚠ 분기보고서에서 엉뚱한 내용 |

공식 명칭에 가깝게 쓰면 잡히고, 일상어면 빠진다.
빈 문자열(`''`)까지 나오는 걸 보면 계획기가 topic 자리를 인지는 하는데 채우지 못하는 경우가 있다.

### 노출도 — 스스로 판단하라고 숫자만 둔다

이 결함이 터지는 조건은 **연도·날짜 앵커가 없고 + 공시 유형을 일상어로 부르는** 질문이다.
앵커가 있으면 topic 이 없어도 후보가 그 해로 좁혀지고, 공식 명칭이면 topic 이 붙는다.

- 제출 셋 58문: 해당 4문. 그중 2문은 프롬프트 공격 문항(TR-ATK-001/002)이고,
  나머지 2문(T4-O-003, T3-C-005)은 계약상대 고유명사가 있어 지금 **통과 중**이다. → 실질 0.
- 심사 질의가 어떤 표현을 쓸지는 알 수 없다. 우리 질의셋은 우리가 공식 명칭으로 썼기 때문에
  **58/58 이 이 결함의 부재를 증명하지 않는다**는 점만 짚어둔다.

### 곁가지 — 모호 질의는 애초에 되물을 수 없다

경욱이 "모호하면 재질의하게 하자"고 물어서 확인한 김에 적는다.
`GET /answer` 는 단발 호출이고 세션도 대화 턴도 없다. 되물어도 심사자는 그 문장을 **답변으로 채점**한다.
그래서 우리가 할 수 있는 건 재질의가 아니라 "해석을 밝히고 답하기"뿐이다.
지금은 그것도 없다(`server/app/` 에 모호성 관련 코드 0건, `SYNTH_SYSTEM` 에도 지침 없음).
다만 실측해 보니 **진짜 모호한 질문**("그 회사 작년 매출은?")은 오히려 무난하게 기권했고,
사람이 읽으면 하나로만 읽히는 **안 모호한 질문**이 위 A/B 처럼 터졌다.
그래서 이 항목은 "모호성 처리"가 아니라 **topic 추출 문제**로 적어둔다.

---

## 구멍 6 — 근거 표시가 5문에서 빠진다 (`citation_display` < 1.0)  ★과제 **필수 요건**

과제소개자료 p.7 평가지표 마지막 줄이 **"모든 답변에는 근거 공시를 표시할 것"** 이다.
정확성·근거완전성 같은 항목별 지표가 아니라 **전 답변에 걸리는 요건**으로 따로 적혀 있다.

우리 `run_eval.py` 는 이걸 `citation_display` 로 이미 재고 있는데,
`answer_ok 미반영` 보조지표라서 **58/58 이 이걸 통째로 가린다.**

dev 58문 중 5문이 1.0 미만이다 (`results_20260902_172136.jsonl`):

| qid | 유형 | cite | ev | 상태 |
|---|---|---|---|---|
| **T5-C-004** | 5 | **0.00** | 1.00 | 근거 표시 **전무** |
| T6-O-001 | 6 | 0.50 | 1.00 | 절반 |
| T6-O-002 | 6 | 0.50 | 1.00 | 절반 |
| T4-O-012 | 4 | 0.50 | 1.00 | 절반 |
| T4-O-016 | 4 | 0.60 | 0.80 | 절반 |

다섯 문항 전부 **정답 판정은 통과**다. 검색도 됐다(ev 0.8~1.0).
답변 문장에 접수번호나 (접수일+보고서명)을 안 적은 것뿐이다.
유형별 평균으로는 유형5 0.80 / 유형6 0.88 / 유형4 0.95 로 찍힌다.

### 판정에 불확실한 부분 — 그대로 적어둔다

"답변"이 `answer` 필드만인지 응답 5필드 전체인지는 자료에 안 나온다.
`retrieved_context` 에는 근거가 항상 들어가므로, 심사자가 응답 전체를 본다면 문제가 없을 수도 있다.
다만 p.7 의 다른 축(근거 완전성)이 이미 "검색 근거에 포함했는가"를 따로 보고 있어서,
이 마지막 줄은 **답변문 자체**를 가리킬 개연성이 높다고 본다. 어느 쪽이든 표시해서 손해는 없다.

### 고칠 범위

검색·판정은 건드리지 않는다. `answer` 조립 단계에서 근거 표기가 붙었는지만 보면 된다.
지금도 대부분 문항은 `[근거 N/M | 기업명] …` 형태로 잘 붙는다 — 47/52 는 1.0 이다.
빠지는 건 **여러 근거를 한 문단으로 합쳐 서술할 때**로 보인다(위 5문 전부 다건 비교·정리형).

---

## 통과했지만 짚어둘 것 — T5-C-012

부재 함정(대우건설 영업정지 집행정지 신청 **결과** 공시는 코퍼스에 없다)은 정확히 잡아
"찾을 수 없으므로 답변드릴 수 없습니다"로 답했다. **결론은 옳다.**

다만 근거 3건 중 2건이 무관했다 — 신흥3구역 시공자 선정(`20251201800043`),
수원당수2 우선협상대상자(`20251212800138`). 맞는 문서는 `20251216000493` 하나뿐이다.
결론이 맞아 통과했을 뿐 검색은 절반이 헛돌았다.

---

## 채점 관련 메모

`run_eval.py`는 `closed`를 `any(accept_patterns)`로, `open`을 **60% 커버리지**로 채점한다.
따라서 **open + accept_patterns 정확히 2개 = 둘 다 맞아야 통과**(1/2 = 0.5 < 0.6).
hard_v1의 "양 끝단" 문항 5개(T5-C-005/006/007/009, T6-O-011)가 이 성질을 쓴다.
일부러 그렇게 설계한 것이니 패턴 개수를 임의로 늘리면 난도가 사라진다.

T5-C-012는 최초 실행에서 내 accept_patterns가 좁아 오답 처리됐다(파이프라인 답은 옳았다).
`"찾을 수 없"` 등 4개를 추가해 고쳤고, 그래서 서버 실행 시점 3/10 → 재채점 4/10이다.
나머지 6건은 답변 전문을 다 읽고 **진짜 실패**임을 확인했다.

## 우선순위 제안 (판단은 선우가)

프리즈 9/6까지 4일이고 dev 58문이 100%라 큰 수술은 위험하다는 점을 감안한 순서다.

1. **근거 표시 누락** (구멍 6) — 과제소개자료 p.7 의 **명시적 필수 요건**이고,
   우리 계측기에 이미 잡혀 있는데 `answer_ok` 에 안 들어가서 58/58 에 가려져 있었다.
   5문(T5-C-004 는 0.00)이고, 검색·판정은 멀쩡하니 **답변 조립 단계만** 손대면 된다.
   회귀 위험이 제일 낮은데 요건 성격은 제일 무겁다. 그래서 1번에 뒀다.
2. **금액 보강 각주** (구멍 3) — 유일하게 **제출 셋 13문에 이미 붙어 있다.**
   점수는 안 깎이지만 정성 평가에서 손해다. `_postfix` 한 곳이고 회귀 위험이 제일 낮다.
   **주의: 정정전→정정후 칸만 고치면 절반만 고쳐진다.** 검증 3건 중 TR-NAME-001은
   칸이 아니라 **문서**를 틀린 건이라 `plan.py:1044` 의 `full` 뭉치기를 손대야 덮인다.
   가장 작은 완전 수리는 "근거 2건 이상이면 각주 미발화".
3. **자신 있는 오답 억제** (구멍 1의 T5-C-011형) — 질문이 지목한 계약상대와 근거의 계약상대가
   다르면 답하지 말고 부재 처리. 지금은 방향이 반대인 계약(HMM=공급자)을 발주 건으로 낸다.
4. **정정 체인 top-k 상향** (구멍 2) — 체인 문서는 절단 대상에서 제외.
5. **결정 vs 실행 판정 규칙** (구멍 4) — "예정/결정"과 "실행/확정"이 한 문맥에 오면 실행본을 사실로.
6. **교차기업 도달 수단** (구멍 1 본체) — 제일 크고 제일 위험하다. 프리즈 후로 넘겨도 무방하다.
7. **`topic` 추출 강건화** (구멍 5) — 순위를 일부러 아래에 뒀다. 제출 셋 실질 노출이 0이고
   `PLAN_SYSTEM` 은 58/58 전체가 지나는 길목이라 프리즈 4일 전 수정은 회귀 위험이 가장 크다.
   손댄다면 프롬프트 문구보다 **코드 쪽 topic 백필**(일상어→공식 명칭 매핑)이 영향 범위가 좁을 것 같은데,
   어느 쪽이 싼지는 파이프라인을 쥔 선우가 판단하는 게 맞다.

8. **코퍼스 밖 안내·근거 없는 수식 제거** — 과제소개자료 p.5 데이터 활용 규칙에
   `제공 코퍼스 외 데이터 사용 불가`, `공시에 근거 없는 미래 예측·투자 의견 생성 금지` 가 **금지 항목**으로 박혀 있다.
   실측에서 두 건이 그 근처에 있었다:
   - T5-C-010 답변의 **"SK하이닉스의 공식 발표나 보도자료를 참고하시는 것을 권장"** — 코퍼스 밖으로 안내한다.
   - "삼성전자 실적 어때?" 답변의 **"입지를 더욱 공고히"** — 공시에 없는 평가성 수식이다.
   둘 다 점수 계산엔 안 잡히지만 p.7 `안전성 및 신뢰성` 축에서 읽힐 수 있다. 문장만 빼면 된다.

---

# 조치 내역 (선우, 2026-09-03)

## 구멍 3 — 수리 완료 ★

제안 중 **"문서 단위로 훑기"** 를 택했다. "근거 2건 이상이면 미발화"도 검토했는데,
각주 목적(근거에 있는데 답변이 빠뜨린 금액 되붙이기 — T6-O-005 가 이걸로 살았다)을
버리지 않고도 오인을 막을 수 있어서다. 셋을 겹쳐 걸었다.

1. **문서 오인** — `full` 뭉치기를 없애고 item 하나씩 훑는다.
2. **인용 제한** — 답변이 `(근거 N)` 으로 지목한 문서의 값만 쓴다. 인용이 없으면 각주를 안 붙인다.
   (출처를 말할 수 없는 값을 "근거 원문"이라 적는 게 결함의 뿌리라서. 인용 없는 답변에서
   각주가 사라지는 건 의도된 손실이다 — dev 12문 전부 각주 없이도 본문으로 통과함을 먼저 확인했다.)
3. **칸 오인** — 정정 표식이 있는 문서에서는 라벨 뒤 **구분자만 사이에 두고 연달아 붙은**
   금액 나열의 마지막(정정 후)을 쓴다.

**수리 중 하나 더 나왔다.** 3번을 처음에 "라벨 뒤 160자 안 마지막 금액"으로 잡았더니
TR-NAME-001 에서 `계약금액(원) 198,437,328,900 최근매출액(원) 2,220,751,868,438` 의
**최근매출액**(다른 항목)을 집었다. 원문 대조로 잡았고, 한글 라벨이 나오면 나열을 끊게 고쳤다.
경욱이 형이 검증한 3건(TR-NAME-001·T5-C-005·T6-O-010)을 실제 원문 형태 그대로
`agent/test_footnote.py` 픽스처로 박았다 — **픽스처를 원문보다 단순하게 만들면
테스트가 통과해도 버그가 산다**는 걸 이번에 배웠다.

검증: test_footnote 6케이스 + 기존 postfix 테스트 회귀 전부 통과, dev 58/58 유지(재측정).
**서버 반영은 재빌드 필요 — 경욱이 형, 다음 빌드에 포함해 주세요.**

## 구멍 4 — 수리 완료 (결정 vs 실행, 2026-09-03)

T5-C-008 은 검색이 아니라 판정 문제였다(ev 1.00인데 예정일을 답함). 상시 프롬프트
규칙은 무관 문항을 깨뜨린 전례가 있어 **조건부**로 걸었다: 질문이 "실제로/실제
일어난/결국 언제"를 물을 때만 합성 지시에 "실제로 일어난 일은 실행·완료 공시의 값이다.
예정값은 '당초 예정'으로만 구분하라"를 붙인다(`plan.py synthesize()`).

발동 조건 전수 검사: dev 58문 0건 · 변형 232문 0건 · hard_v1 은 T5-C-008 하나만 —
회귀 경로가 구조적으로 막힌 걸 확인하고 넣었다.

검증: T5-C-008 단독 3회 전부 정답("2025년 2월 13일 매매거래 종료", must_not 위반 0),
hard_v1 **4 → 5/10**, dev 55/58(실패 3문 전부 429 — 429 무관 회귀 0건).
남은 hard 5문은 아래 보류 항목(교차기업 4 + 체인 절단 1) 그대로다.
**이것도 서버 반영은 재빌드 필요.**

## 구멍 1·2 — 프리즈 후로 보류 동의

교차기업은 제안대로 프리즈 후. 체인 top-k 는 dev 재측정 결과를 보고 판단.
"자신 있는 오답 억제"(우선순위 2)는 구멍 4와 같이 본다.
