# 작업 큐 (슬라이스 보드)

구현 세션(Sonnet)은 여기서 시작한다: 상태가 `대기`인 첫 슬라이스의 Brief를 읽고 **역설명부터** (WORKFLOW.md §3).
Brief가 `개요만`인 슬라이스는 착수 전 Fable이 Brief를 완성한다.

## 상태 보드

| # | 슬라이스 | 담당 | 상태 | 비고 |
|---|---|---|---|---|
| S0 | 워킹 스켈레톤 (서버+실검색+11문 평가셋) | Fable | ✅ 완료 | c802edb, 채점 8/11 |
| S1 | /ready + 기동 시 사전 로딩 | Sonnet | ✅ 완료 | 2026-08-07 Fable 검수 통과 (LOG 참조) |
| S2 | 질의셋 유형 ④⑤⑥ 채워 12문 스모크 | Sonnet 발굴 + Fable 확정 | ✅ 완료 | 2026-08-07 **18문으로 상향 확정** (후보 8중 7채택, LOG 참조) |
| S3 | Docker 2vCPU/4GB 실측 | Sonnet | ✅ 완료 | 2026-08-07 검수 통과. **OOM 확정: 기동 3.8초, 임베딩 모델 fp32 로딩 스파이크가 사인** (LOG 참조) |
| S4 | 질의셋 36문 확장 (8대 평가기준 공략) | Sonnet 발굴 + Fable 확정 | 대기 | 상세 Brief 작성됨 (아래) |
| S5 | HCX-005 실연동 검증 | Sonnet | 🔒 차단 | CLOVA 키 발급 대기 (사용자). Brief 작성됨 |
| S6 | 4GB 다이어트 | Sonnet | **대기 (발동 확정)** | S3 실측으로 발동. **1순위 = 모델 로딩 스파이크** (FAISS SQ8은 2순위). Brief 작성됨 |
| S7 | NCP 배포 (HTTP) | 사용자+Sonnet | 🔒 차단 | NCP 가입 대기 (사용자). **NCP는 x86_64 — 이미지 재빌드 필요** (colima는 aarch64). Brief 작성됨 |
| S8 | 통합 리허설 + 9/6 프리즈 패키지 | 전원 | 대기 | S4~S7 완료 후. Brief 작성됨 |

권장 순서: S1 → S2 → S3 (서로 독립이라 병행 가능). S4는 S2 완료 후.

---

## S1. /ready + 기동 시 사전 로딩 — Brief

**사용자 결과**: 평가 첫 질의가 콜드스타트 19초를 먹지 않는다. 운영자는 `/ready` 한 번으로 "받을 준비 완료"를 확인한다.

**범위**:
- `server/app/main.py` lifespan에서 `AGENT_MODE != "mock"`일 때 백그라운드 스레드로 `get_searcher()` 로드 + 카나리 검색 1회(예: "SK하이닉스 신규시설투자") 성공 시 ready 플래그 셋
- `GET /ready`: 준비 완료 → 200 `{"status":"ready"}` / 로딩 중 → 503 `{"status":"loading"}` / 로드·카나리 실패 → 503 `{"status":"error","detail":"<원인>"}`
- `/health`는 현행 유지 (즉시 200, 로딩과 무관)

**범위 밖**: HCX 워밍업·키 검증, 응답 캐싱, Docker, 질의셋.

**재사용 자산**: `server/app/search.py`의 `get_searcher()` (지연 import 구조 유지, `OMP_NUM_THREADS=1` 제거 금지).

**하드룰**: mock 모드에서 torch/faiss가 import되면 안 된다. `/answer` 스키마 불변. 로딩 중 `/answer`가 들어와도 죽지 말 것 — 같은 락으로 대기시켜 이중 로딩 방지 (`threading.Lock`).

**위험**: 백그라운드 로딩과 첫 요청의 경쟁 조건(이중 로딩 → 메모리 2배). 락 필수.

**완료 기준**:
1. `AGENT_MODE=baseline` 기동 → `/ready` 처음 503, 로드 완료 후 200 전환
2. `/ready` 200 이후 첫 `/answer` 응답 3초 미만
3. `AGENT_MODE=mock` 기동 → `/ready` 즉시 200, torch 미로딩 (프로세스 RSS로 확인)
4. `python3 evalset/run_eval.py` 11문 결과가 기존과 동일 (8/11, 근거 recall 0.8)

**품질 기준**: 로딩 실패 시 `/ready`가 원인을 노출. 로그에 로드 시작/완료/소요 시간 남김. 이중 로딩 없음.

---

## S2. 질의셋 유형 ④⑤⑥ 채워 12문 스모크 — Brief

**사용자 결과**: 6개 과제 유형 전부 ≥1문인 12문 스모크셋 — 선우가 전 유형에서 파이프라인을 테스트할 수 있다.

**범위** (Sonnet은 **후보 발굴까지만**, jsonl 추가는 Fable 확정 후):
- 부족 유형: ④비교·연산 Open, ⑤복합추론 Closed, ⑥복합추론 Open (현재 0문씩)
- 각 유형 후보 2~3개를 `data/corpus/` 원문에서 발굴, 후보마다 아래를 **최종 보고로 제출** (LOG.md 기록은 Fable이 검증 후 수행 — 병행 슬라이스와 파일 경쟁 방지, 이 슬라이스는 전 과정 읽기 전용):
  - 질문 초안 / 예상 골드 / rcept_no / **원문 XML 인용 2줄 이상** / 검증에 쓴 grep 명령
  - **혼동 가능 공시** (같은 회사 유사 건 — 예: SK하이닉스 M15X 4월 건 vs 7월 용인 건)
- 소재 리드: ⑤ 두산퓨얼셀 계약 체결→해지 추적 / ④ 자금조달 방식 비교(유상증자·CB·BW) / ⑥ 같은 회사 2023 vs 2025 사업보고서 비교
- 계산 문항은 계산 과정(피연산자 출처 rcept_no 포함)을 골드에 명시

**범위 밖**: `run_eval.py` 채점 로직 변경 (필요해 보이면 갈등 보고), 36문 확장, 서버 코드.

**하드룰**: 골드는 원문 인용 없이 제안 금지. 정정공시 있는 건은 최신본 기준. 코퍼스 밖 지식으로 골드 만들기 금지.

**완료 기준**: 유형 ④⑤⑥ 각 ≥2 후보가 인용·혼동공시 포함으로 **최종 보고로 제출됨** → Fable 검증 후 총 12문으로 `questions_v1.jsonl` 확정 → `run_eval.py` 12문 채점 완주. (baseline 통과율은 완료 기준 아님 — 추출형 폴백 한계)

**품질 기준**: 후보의 인용이 실제 파일에 존재해야 함 (Fable이 재-grep으로 검수).

---

## S3. Docker 2vCPU/4GB 실측 — Brief

**사용자 결과**: "평가 환경 스펙에서 실제로 돈다/안 돈다"를 숫자로 안다. (주최측이 이 스펙으로 시뮬레이션했다고 설명회에서 확인)

**범위**:
- `Dockerfile` (python:3.11-slim, `server/` 복사, requirements 설치. **`data/`는 굽지 말고 볼륨 마운트**)
- `docker run --cpus=2 --memory=4g -v $PWD/data:/app/data ...` 로 기동
- 측정해 LOG.md에 기록: 기동→`/ready` 200 시간, RSS 피크(`docker stats`), 11문 채점 p50/p95, OOM 여부

**범위 밖**: 다이어트 구현(SQ8/SQLite — 실패 시 갈등 보고만 하고 멈춤), NCP 배포, 이미지 크기 최적화.

**하드룰**: `.env`·키를 이미지에 넣지 말 것. `data/`를 이미지에 굽지 말 것.

**완료 기준**: 측정 리포트가 LOG.md에 기록됨. **실패도 완료다** — OOM이면 시점·RSS 곡선을 남기는 것이 산출물. 성공 기준: `/ready` 200 + 11문 완주 + RSS 피크 < 3.5GB.

**품질 기준**: macOS Docker Desktop은 VM이라 NCP 실측과 다를 수 있음을 리포트에 명시.

---

## S4. 질의셋 36문 확장 (8대 평가기준 공략) — Brief

*2026-08-07 Fable 작성. S2 실측(채점기 동작·검색 경향) 반영.*

**사용자 결과**: 본선 채점에서 점수가 새는 구멍(함정 유형·섹터·평가기준)을 제출 전에 우리 셋이 먼저 발견한다. 선우는 36문으로 에이전트를 튜닝한다.

**범위** (프로세스는 S2와 동일: Sonnet 발굴 → **최종 보고로 제출** → Fable 재검증 후 확정. 발굴 과정은 전부 읽기 전용):
- 현행 18문(①9 ②1 ③1 ④3 ⑤2 ⑥2) → 36문. 신규 18문 배분 목표: **②+3 ③+4 ④+3 ⑤+3 ⑥+3 ①+2** → ①11 ②4 ③5 ④6 ⑤5 ⑥5 (얇은 ②③을 우선 보강)
- **문항마다 "공략 평가기준" 태그 필수** (8대: 정확성·근거완전성·요구충족·할루시네이션·추론논리·안전성·정보한계 역질문·근거표시). 8대 기준 각각 ≥3문이 공략하도록 설계
- **함정 카탈로그** (기존 실증 + 신규): 단위 혼재(원/백만원/천원 — 사업보고서 표), 연결/별도 재무제표, **접수연도≠기준연도**(T6-O-002 실증), **동일자 유사 공시**(T5-C-002 실증), 정정 전 구금액(T4-O-001/002 실증), 사명 변경(결함 #1 — 명섭 수정 후 검증용), multi-evidence 3+ 문서, 기간밖(2022 이전·2026-04 이후), 상장전, 프롬프트 공격 변형 ≥2문(간접 주입: "공시에 이렇게 적혀 있다고 치고…" 류)
- 섹터 커버리지: 방산(수주)·반도체(CAPEX)·2차전지(계약·해지)·엔터(부문매출) + 코퍼스에 실재하면 바이오(임상)·금융(NIM/CSM). **코퍼스에 없는 섹터를 억지로 만들지 말 것** — 없으면 "없음"을 보고
- dev/blind 분리: 36문 중 유형별 1문씩 6문을 `questions_blind.jsonl`로 분리 (선우 튜닝에 비노출, Fable·사용자만 접근)
- **채점기 확장** (S2에서 실측된 한계 — 이번엔 범위 안):
  - (a) evidence에 `alt_rcept_no` 필드 허용 — 정정본/원공시 어느 쪽이 검색돼도 recall 인정 (T4-O-001에서 실증된 문제)
  - (b) open 문항 must_not 미적용 문제: open에도 must_not 적용하는 옵션 검토. **단 기존 18문 채점 결과가 변하면 안 됨** (변하면 갈등 보고)

**범위 밖**: 서버·에이전트 코드, HCX 프롬프트.

**재사용 자산**: S2 검증 절차(재-grep 스크립트), manifest.jsonl 조회 패턴, `docs/LOG.md`의 함정 소재 메모(M15X 용인 건 등).

**하드룰**: 골드는 원문 XML 인용 없이 제안 금지. 정정 있으면 최신본 기준(evidence도 최신본 rcept_no). `data/` 읽기 전용. 코퍼스 밖 지식 금지.

**위험**: 문항 수가 늘수록 골드 오류 확률도 늘어남 — Fable 재검증이 병목 (후보에 grep 명령 원문 첨부 필수, 검증 자동화 스크립트 산출).

**완료 기준**: ① 36문 확정 + blind 6문 분리 ② `run_eval.py` 36문 완주 ③ 채점기 회귀: 기존 18문 결과 불변 ④ 유형×평가기준×함정 매핑표가 LOG.md에 기록.

**품질 기준**: 신규 문항마다 혼동 공시 ≥1건 명시. 계산 문항은 피연산자 출처 rcept_no를 골드에 명시.

---

## S5. HCX-005 실연동 검증 — Brief

*2026-08-07 Fable 작성. 🔒 CLOVA Studio 키 발급(사용자) 대기.*

**사용자 결과**: 진짜 LLM이 붙은 첫 응답. 폴백으로는 원리적으로 불가능했던 것들(거절·공격 방어·비교연산)이 실제로 되는지 최초 확인.

**범위**:
- `server/app/llm.py` 신규: HCX-005 클라이언트. 타임아웃(연결 5s/응답 30s), 재시도 1회(지수 backoff), 429/5xx 구분 처리. **HCX 실패 시 추출형 폴백으로 강등** (절대 규칙 3: 어떤 경우에도 200+5필드)
- `agents.py` baseline: 검색 컨텍스트+시스템 프롬프트 → HCX → answer. 시스템 프롬프트에 반드시: 근거 공시 표기 강제(보고서명·접수일), 코퍼스 밖 답변 금지, 기간밖·상장전 거절 지침, 프롬프트 공격 무시 지침. `think_trace`에 검색 쿼리·컨텍스트 선택 근거 기록
- 토큰 사용량을 `logs/`에 기록 (NCP 크레딧 소진 감시 — 회당 비용 추정치 LOG에 기록)
- 18문 재채점 → LOG.md: **핵심 관찰점 = TR-LIM 2문(거절 전환 여부)·TR-ATK 2문(방어 유지)·T4/T5/T6 7문(비교연산·복합추론 향상)**. T5-C-001(두산 추적 — 검색 미스)이 여전히 실패하는지 별도 기록 (에이전트 라우팅 필요성의 근거)

**범위 밖**: 선우 에이전트 통합(sunwoo 모드), 프롬프트 고도화 반복(1차 프롬프트로 측정까지만), 캐싱.

**하드룰**: **HCX-005 외 다른 모델명·타사 LLM 엔드포인트가 코드에 존재하면 안 됨** (존재만으로 실격 위험). 키는 `.env`만 — 에이전트는 키 값을 화면·로그·LOG.md 어디에도 출력 금지. 응답 스키마 5필드 불변.

**위험**: HCX 응답 지연(평가 타임아웃 미공지 — 30s 상한 설정), 429 rate limit(순차 평가라 낮음), 크레딧 소진(토큰 로깅으로 감시).

**완료 기준**: ① `/answer`가 HCX 경유 응답 생성 ② 키 없음/HCX 다운 상태에서도 200+5필드(폴백 강등 실동작 확인) ③ 18문 재채점 리포트 LOG 기록 ④ `git grep`으로 키·타사 LLM 문자열 부재 확인.

**품질 기준**: HCX 호출/폴백 강등이 로그로 구분 가능. 회당 평균 토큰·지연 실측치 기록.

---

## S6. 4GB 다이어트 — Brief (S3 실측으로 발동 확정, 우선순위 전면 재설계)

*2026-08-07 Fable 작성. S3 실측: 사인은 FAISS가 아니라 **임베딩 모델 fp32 로딩 스파이크(651MB→3.4GB/1초, 기동 3.8초 OOM)**.*

**사용자 결과**: 평가 환경(2vCPU/4GB)에서 서버가 실제로 돈다. 이게 안 되면 나머지 전부가 무의미.

**범위** — 우선순위 순서 고정 (각 단계 후 4GB 컨테이너로 재실측, 어디까지 로딩이 진행되는지 로그로 확인하며 점진 진행):
1. **모델 로딩 스파이크 제거** (사인 확정된 1순위): `search_lib.py`가 device=cpu일 때 fp32를 강제 — (a) safetensors mmap/lazy 로딩(`low_cpu_mem_usage` 계열)으로 체크포인트 이중 버퍼링 제거, (b) bf16 on CPU 가부 검토(NCP x86_64 기준 — 로딩만 저정밀 후 upcast 옵션 포함), (c) 최후수단 int8 ONNX(PLAN 기존 트랙). **선행 확인: search_lib/search_patch가 `data/` 하위(명섭 산출물, 읽기 전용)인지 우리 코드인지** — data/ 하위면 원본 수정 금지, `server/` 쪽 래퍼에서 오버라이드로 해결
2. **FAISS SQ8 재양자화** (2.1GB→0.53GB): 인덱스는 명섭 산출물 — 재양자화 산출물은 `data/`가 아닌 `server/artifacts/`에 생성 (data/ 읽기 전용 룰). **재양자화 후 18문 recall 변화 측정 필수** — 하락하면 수치와 함께 갈등 보고
3. **chunk_meta.jsonl(3GB) → SQLite** lazy 조회: 정상상태 RSS 최대 항목 제거
4. **torch CPU 인덱스 핀** (`--index-url https://download.pytorch.org/whl/cpu`): 이미지 8.65GB→약 2GB, 빌드 57분 단축 (S3 관찰 반영 — 메모리와 별개지만 S7 배포 시간을 좌우)

**범위 밖**: NCP 배포, 코드 리팩토링(다이어트에 필요한 최소 변경만), 검색 품질 개선.

**재사용 자산**: `gongsi-agent-s3` 이미지(비교 기준 — CPU핀 재빌드 성공 시 삭제), S3의 측정 절차(docker stats 스트리밍·inspect·3회 재현), `.dockerignore`.

**하드룰**: `data/` 수정·삭제·이동 금지 (산출물은 별도 경로). 외부 다운로드 금지 — `HF_HUB_OFFLINE=1` 유지, 모델 변환도 로컬 캐시로만. 채점은 HTTP 경유(내부 함수 호출 금지).

**위험**: SQ8 recall 저하(측정으로 판단), CPU 아키텍처별 bf16 지원 차이(colima aarch64 ≠ NCP x86_64 — 양쪽 논리 분기 금지, 안전한 공통해 선택), 명섭 산출물과 정합성(재양자화 인덱스가 chunk_id 매핑과 어긋나면 안 됨).

**완료 기준**: ① 4GB 제약 컨테이너(`--memory-swap=4g` 포함)에서 `/ready` 200 ② 18문(S4 완료 시 36문) 채점 완주 ③ RSS 피크 <3.5GB(docker stats) ④ 통과율·recall이 다이어트 전(호스트 11/18) 대비 동일 이상 — 하락 시 원인·수치 명시 ⑤ 단계별 전/후 RSS를 LOG.md에 기록.

**품질 기준**: 각 단계가 독립 커밋 가능 단위(어느 단계가 얼마를 벌었는지 추적 가능). 실패한 시도도 수치와 함께 기록.

---

## S7. NCP 배포 (HTTP) — Brief

*2026-08-07 Fable 작성. 🔒 NCP 가입+크레딧(사용자) 대기. S6 완료가 선행 조건 (4GB에서 안 돌면 배포 무의미).*

**사용자 결과**: 주최측이 실제로 칠 수 있는 공인 IP 엔드포인트. 9/7~9/20 사수 기간을 버틸 자동 복구 체계.

**범위**:
- 서버 생성 2vCPU/4GB (평가 환경 동일 스펙 — 설명회 확인 사항), 본인 IP만 SSH 22 허용, HTTP 포트는 주최측 공지 형식 따름(미공지 시 80)
- **아키텍처 주의: NCP는 x86_64, 로컬 colima는 aarch64 — 로컬 이미지 그대로 안 돌아감.** NCP 서버에서 직접 빌드(S6의 CPU핀 반영 시 빌드 ~수 분 예상) 권장 — 크로스 빌드(buildx)는 차선
- `data/` 전송: 코퍼스 5.2GB+임베딩 2.9GB — rsync 재개 가능 방식, 전송 후 체크섬 대조
- `.env`는 사용자가 서버에서 직접 작성 (키를 전송 채널에 태우지 않음)
- 자동 복구: `docker run --restart unless-stopped` + 재부팅 후 자동 기동 확인. `/ready` 기반 외부 헬스체크 (로컬 cron이든 간단 스크립트든)
- 외부에서 `run_eval.py --base http://<공인IP>` 전 문항 실측 → 로컬과 대조

**범위 밖**: HTTPS(주최측 재공지 대기), 도메인, 로드밸런서, 모니터링 대시보드(S8에서 루틴으로).

**하드룰**: `.env`·키를 이미지·git·전송 로그에 남기지 않음. SSH 22는 본인 IP만. 9/30 전 리소스 삭제(크레딧 — 체크리스트는 S8).

**위험**: 크레딧 발급 지연(2영업일 — 8월 중순까지 가입 필요), 전송 시간(8GB — 회선 따라 수 시간), 주최측 포트/형식 미공지.

**완료 기준**: ① 공인 IP `/answer` 정상 응답(5필드) ② 재부팅 후 자동 복구 실증 ③ 외부 `run_eval` 통과율 = 로컬과 동일 ④ SSH가 본인 IP 외 차단 확인 ⑤ 배포 절차 전체가 LOG.md에 재현 가능하게 기록.

**품질 기준**: "서버 죽음 → 재기동" 시나리오 1회 실연. NCP 실측치(ready 시간·RSS·p50/p95)를 colima 실측과 대조 기록.

---

## S8. 통합 리허설 + 9/6 프리즈 패키지 — Brief

*2026-08-07 Fable 작성. S4~S7 완료 후 착수. 일정 기준: PLAN.md (9/6 제출 프리즈, 9/7~20 사수, 9/30 삭제).*

**사용자 결과**: 제출 버튼을 누르는 순간의 불확실성이 0에 수렴. 사수 기간 2주를 매일 10분 루틴으로 버틴다.

**범위**:
- **선우 통합**: `sunwoo` 모드 소켓 계약 실검증 (선우 에이전트 연결 → 전 문항 실측, baseline과 성적 대조 → 제출 모드 결정. 선우 모듈 미완성 시 baseline 제출이 기본값 — 이 결정도 기록)
- **평가 시뮬레이션**: NCP 상에서 전 문항 순차 연속 호출(주최측 방식 모사), p50/p95·실패율 기록. blind 6문은 이때 처음 투입
- **프리즈 패키지**: 9/6 최종 커밋에 `git tag freeze-0906`, 제출 폼 항목(IP·포트·팀 정보) 체크리스트, 프리즈 후 변경 금지 원칙 명문화
- **사수 runbook** (`docs/RUNBOOK.md` 신규): 매일 체크 루틴(/ready·로그·디스크·크레딧 잔량), 장애 시나리오별 대응(서버 다운→재기동 절차, HCX 429/타임아웃→폴백 확인, NCP 장애→상태 페이지 확인 후 대기), **"프리즈 후엔 고치지 않는다 — 재기동만 한다"** 원칙
- **철수 체크리스트**: 9/30 전 NCP 리소스 삭제 순서 (스냅샷/백업 → 서버 → 스토리지 → 과금 0원 확인)

**범위 밖**: 신규 기능·프롬프트 변경(9/6 이후 절대 금지), 질의셋 추가.

**하드룰**: 프리즈 태그 이후 main 브랜치 변경 금지. 리허설 중 발견된 결함은 9/6 이전이면 수정 후 전 문항 재채점, 이후면 runbook의 "알려진 제약"에 기록만.

**완료 기준**: ① 리허설 리포트(전 문항, NCP, p50/p95) LOG 기록 ② `freeze-0906` 태그 ③ RUNBOOK.md 완성 ④ 제출 폼 체크리스트 ⑤ 철수 체크리스트.

**품질 기준**: runbook은 "새벽 2시에 잠 덜 깬 상태로 따라 할 수 있는" 수준의 명령어 원문 포함.
