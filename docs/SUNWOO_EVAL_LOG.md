# 평가셋 점수 개선 일지

팀 평가셋 30문항(`miraeasset-NyangNyang/evalset/questions_v1.jsonl`)을 우리 파이프라인으로
직접 채점하며 올린 기록. 채점기는 팀 레포의 `run_eval.py` `grade()`를 그대로 import해 쓴다.

**실행:** `cd agent && python eval_local.py` (전체) / `python eval_local.py T4` (유형별) /
`python eval_local.py T4-O-004,T5-C-001` (문항 지정)

**기록 규칙:** 실험마다 ①무엇을 바꿨나 ②왜(근거) ③측정 결과 ④판정을 남긴다.
점수가 안 오르거나 떨어진 실험도 반드시 남긴다 — 같은 길을 두 번 가지 않기 위해서다.

---

## 점수 추이

| 시점 | 점수 | 비고 |
|---|---|---|
| 팀 baseline | 17/30 | 경욱 형이 `AGENT_MODE=baseline`을 **HCX 키 없이** 돌린 값. 추출형 폴백이라 우리와 직접 비교 불가 |
| 우리 1차 | 20/30 | 최초 측정 |
| 실험 1~2 | 20/30 | 변동 없음(복구 1 + 퇴행 1) |
| **우리 2차** | **22/30** | 슬롯 캐시 + 라우터 보정 + 월/용어 필터 + 해지 연도필터 제거 |
| **우리 3차** | **24/30** | 실패 8문 병렬 진단 반영 — 유형④ 5/5 완봉, 정정공시 함정 4/4 |
| **우리 4차** | **25/30** | 유형⑥ 사업보고서 당기 열 코드 추출 (`gather_annual`) |
| **우리 5차** | **24/30** | `PatchedSearcher` 경유로 전환(팀 규약 필수). T2-O-003 1건 퇴행 |
| baseline (HCX 붙임) | **20/30** | 공정한 비교 기준선. 2026-08-10 직접 측정 |
| **우리 6차** | **28/30** | 라우팅 3종 + 정정본 교체 + 체인 연도 + 표 합계 인용 |
| **서버 경유 실측** | **28/30** | `AGENT_MODE=sunwoo` + `GET /answer`. 실제 평가 경로에서 동일 |
| **다이어트 켜고 실측** | **28/30** | RSS **2.57GB** — 4GB 목표 통과. 배포 블로커 해소 |
| **우리 7차** | 측정 중 | T3-C-002 해결 + 평가셋 변형 4종 교차 검증 |

**주의:** baseline 17/30과 우리 점수를 나란히 놓으면 안 된다. 그쪽은 LLM 없이,
우리는 HCX를 쓰고 잰 값이다. 공정한 비교 기준선(HCX 붙인 baseline)은 아직 아무도 측정하지 않았다.
`search_patch.py`를 받아 서버가 뜨는 즉시 그것부터 재야 한다.

---

## 실험 0 — 최초 측정 (20/30)

| 유형 | 점수 | 근거recall |
|---|---|---|
| ① 검색추출C (11) | 10/11 | 0.83 |
| ② 검색추출O (3) | 3/3 | 1.00 |
| ③ 다중비교C (3) | 2/3 | 0.83 |
| ④ 다중수집O (5) | 1/5 | 0.70 |
| ⑤ 복합추론C (4) | 2/4 | 0.50 |
| ⑥ 복합추론O (4) | 2/4 | 0.75 |

함정: 프롬프트공격 4/4, 상장전 2/2, 기간밖 1/1, 정정공시 2/4, 사명변경 1/2.
**Closed 15/18(83%) vs Open 5/12(42%)** — Open이 구멍이라는 게 첫 측정에서 드러났다.

실패 10문 중 **7문이 계산(합계·차이·비중)을 요구했고, 그중 5문은 근거recall이 1.0**이었다.
자료는 찾아놓고 마지막에서 떨어진다는 뜻이다.

---

## 실험 1 — 프롬프트 4종 동시 투입 (20/30, 변동 없음)

**바꾼 것**
1. `TABLE_RULES` — "표에 여러 회계연도 열이 나란히 있으니 당기 열만 읽어라"
2. `build_context` 헤더에 기준연도 추가
3. `SCOPE_RULES` — 연결/별도 구분, 외화 표기 수치는 본사 실적 아님
4. slice4 집계 지시 — 질문이 합계·차이를 요구하면 식을 적어 계산
5. slice5 해지 앵커 연도 필터 제거
6. slice5 질문 날짜로 해지 우선 정렬

**결과** T5-C-002 복구(0→1), T6-O-005 퇴행(1→0). 총점 동일.

**판정**
- ⑤ 연도 필터 제거는 **유지**. 진단이 정확했다 — "2024년 체결한 계약 중 해지된 것"인데
  해지 앵커에도 2024 필터가 걸려 2025년 해지 공시가 전멸하고 있었다.
- 집계 지시는 **유지**. 답변에 `303,200,000,000 + 317,300,000,000 + 333,000,000,000 = 953,500,000,000`
  처럼 식이 실제로 나왔다. HCX가 계산을 안 하는 게 아니라 **틀린 문서를 고르고 있었다**는 것이 여기서 드러났다.
- `TABLE_RULES`는 **되돌림**. 유형⑥ 2/4→1/4. "여러 연도 열이 있다"를 "다 적어라"로 읽어
  T6-O-002가 "41.28%, 43.13%, 47.80%"처럼 세 값을 나열했다.

---

## 실험 2 — TABLE_RULES 되돌림 (20/30, T6-O-005 미복구)

되돌렸는데 T6-O-005가 안 돌아왔다. 남은 변경은 기준연도 헤더뿐이라 그걸 의심했다.
**여기서 측정 자체를 의심하기 시작했다.**

---

## 실험 3 — 측정 노이즈 발견과 제거 ★ 이 일지에서 가장 중요한 항목

**발견.** T4-O-004를 같은 코드로 3번 돌렸더니 **실패 · 실패 · 성공**이 나왔다.
`temperature=0`인데도. 게다가 근거recall이 0.67↔1.0으로 흔들렸는데,
slice4는 벡터 검색 없이 manifest를 필터링하므로 결정적이어야 한다.

**원인.** `extract`가 같은 질문에 매번 다른 슬롯을 준다(HCX는 temperature 0에서도 흔들린다).
슬롯이 달라지면 라우팅과 키워드 필터가 달라지고 하류 전체가 달라진다.
실제로 T4-O-004의 슬롯은 회차에 따라 `qtype`이 1과 4를 오갔다.

**조치.** `extract.py`에 질문 → 슬롯 파일 캐시(`slot_cache.json`)를 넣었다.
같은 질문은 같은 슬롯을 쓴다. 3회 반복이 동일해졌다.

**의미.** 이 조치 이전의 모든 판단은 신뢰도가 낮다. "고쳤다/퇴행했다"가 노이즈였을 수 있다.
운영에서도 같은 질문에 같은 답을 주는 편이 낫고 호출도 아낀다.

같이 정리한 것: `extract.py`의 감사 결함 3건 — 경로 하드코딩(CWD 상대경로),
`status_code` 미검사(429/5xx에서 KeyError), 코드펜스 한 줄 응답 IndexError.

---

## 실험 4 — 라우터 보정 + 코드 필터 (22/30) ✅

**바꾼 것**

**(a) 라우터 코드 보정.** 캐시가 잡아준 슬롯을 보니 T4-O-004가 `qtype: 1`이었다.
"3건의 합계"를 묻는데 단일 검색으로 분류돼 slice4를 아예 안 탔다.
→ `main.py`에서 질문에 `\d+건|여러 건|각 건`이 있고 qtype이 1·2면 4로 되돌린다.
프롬프트가 아니라 코드라 항상 같게 동작한다.

**(b) 월 필터 + 질문 용어 필터.** 질문에 "2024년 1월"이라 적혀 있는데 연도로만 걸러
후보 17건을 던지고 있었다. → `17 → 7(1월) → 5(VLGC/VLAC)`로 코드가 좁힌다.
질문에 쓰인 사실로만 좁히므로 추측이 아니다.

**(c) 다중 연도 지원.** `main`이 `years[-1]` 하나만 slice4에 넘겨서
"2023년 유상증자와 2024년 전환사채"처럼 두 시점을 묻는 질문에서 한쪽이 사라졌다.
→ 연도 리스트 전체를 넘기고 `rcept_dt[:4] in years`로 거른다.
월 필터는 **월이 명시된 연도만** 좁힌다(“2023년 초”처럼 월이 없는 연도까지 자르면 그쪽 근거가 통째로 날아간다).

**결과 20/30 → 22/30**

| 유형 | 전 | 후 |
|---|---|---|
| ④ 다중수집O | 1/5 | **2/5** |
| ⑤ 복합추론C | 2/4 | **3/4** |
| Open 전체 | 5/12 (42%) | **7/12 (58%)** |

**왜 프롬프트가 아니라 코드였나.** T4-O-004의 정답 문서 하나는
2025-04-22에 접수된 정정본이다. 청크 본문 첫 줄이 `[HD현대중공업] [기재정정]단일판매ㆍ공급계약체결 (2025.04.22)`로
시작해서, 헤더에 "원공시 20240115"라고 아무리 적어도 **HCX는 본문을 보고 2025년 건이라 판단해 버린다.**
프롬프트로 세 번 시도해 세 번 실패한 뒤 코드로 옮겨 해결했다.

---

## 실험 5 — 실패 8문 병렬 진단 반영 (24/30) ✅

에이전트 8기로 실패 문항을 나눠 코퍼스 원문까지 진단하고, 각 진단을 **다른 에이전트가
데이터로 재현해 검증**했다(16기). 검증 단계가 원안의 오류를 여러 건 잡아냈다 —
"이미 코드에 들어가 있는 수정을 다시 제안", "제안한 dedup이 최대 8문항의 근거를 날림",
"교체하려는 코드가 실은 no-op" 등.

### 적용한 것

**(a) 별도/연결 스코프 보강 (`slice1.scope_backfill`, `scope_rerank`)**
T3-C-002가 삼성화재 별도 당기순이익을 물었는데 싱가포르·영국 법인 표를 집었다.
낱말로는 못 고른다 — **정답이 든 요약별도재무정보 청크에는 '별도'가 0회이고,
오답을 만든 해외법인 표에는 '별도재무제표 기준으로 작성'이 2회 박혀 있다**
(표 제목이 청킹 경계에서 앞 청크로 잘려나간 탓). 그래서 낱말 대신
①단위 선언이 외화뿐인가 ②'연결' 밀도 ③섹션 경로로 층을 나눠 뒤로 민다.
그리고 재정렬만으로는 부족해서(삼성화재 2024 청크가 2,495건이라 정답이 검색 상위에
못 들어옴) `s.meta` 직행 보강을 앞에 붙였다.

**(b) 검색 k 40 → 120 (`slice1`)**
기업·연도 필터가 사후라 후보 풀이 얇다. 정답 청크가 40위 밖이면 뒤의 어떤 정렬도 무의미하다.

**(c) 라우터 보정 2종 추가 (`main`)**
- 유형3인데 기업이 1곳 → 유형1. slice3은 기업 2곳이 아니면 근거 없이 "비교 대상 부족"만
  돌려주고 끝나서 어떤 경우에도 정답이 될 수 없다.
- 유형6인데 단일 기업 + 수시공시 사건 어휘(발행·증자·사채·계약·투자·취득·해지) → 유형4.
  **T4-O-001·T4-O-002가 slice4를 아예 안 타고 있었다** — 검증 에이전트가 slot_cache를 읽고
  "이 문항은 qtype 6으로 라우팅되므로 slice4를 고쳐도 닿지 않는다"고 잡아냈다.

**(d) 유형④ 컨텍스트 예산제 (`slice4`)**
공시 1건당 첫 청크 800자만 넣어서 자금조달 목적 내역·이자율처럼 표 뒤쪽 수치가 잘렸다.
`budget = 14000 // 건수`로 건수가 적으면 넉넉히, 많으면 짧게 — 총 투입량을 일정하게 유지한다.

**(e) 키워드 선택을 데이터 기반으로 (`slice4.pick_keywords`)**
item을 통째로 쓰면 안 된다. HCX가 "신규시설투자 금액"이라 주는데 실제 보고서명은
"신규시설투자등"이라 부분일치가 실패해 0건이 됐다. 별칭 사전 → 없으면 item에서 뽑은 토막 중
**후보 제목에 실제로 등장하는 것만** 남기고, 하나도 없으면 키워드 필터를 아예 안 건다
(엉뚱한 문자열로 0건을 만들어 거짓 부재를 단언하는 것보다 낫다).

**(f) item이 리스트로 올 때 처리 (`main.normalize_slots`)**
extract가 `["매출액","영업이익","수주잔고"]`를 주는데 `str()`로 감싸서
`"['매출액', '영업이익', '수주잔고']"`가 됐다. 검색어와 키워드 필터가 통째로 망가진다. → 공백 join.

**(g) 거부는 짧게 (`attribute.GUARD_RULES`)**
TR-ATK-002가 거부는 했는데 **"목표주가는 애널리스트들이…"라고 설명해버려** must_not
패턴 `목표주가는`에 걸렸다. → "공시에서 확인되지 않습니다 한 문장으로만. 그것이 무엇인지
설명하거나 어디서 찾으라고 안내하지 마라. 요청받은 항목 이름을 반복하지도 마라."

### 결과 22/30 → 24/30

| 유형 | 2차 | 3차 |
|---|---|---|
| ① 검색추출C | 10/11 | 10/11 |
| ② 검색추출O | 3/3 | 3/3 |
| ③ 다중비교C | 2/3 | 2/3 |
| **④ 다중수집O** | 2/5 | **5/5** ✅ 근거recall 1.00 |
| ⑤ 복합추론C | 3/4 | 3/4 |
| ⑥ 복합추론O | 2/4 | 1/4 |
| **정정공시 함정** | 2/4 | **4/4** ✅ |

**유형④를 완봉했다.** 근거recall도 0.70 → 1.00.

### 중간에 냈다가 되돌린 퇴행 3건 (기록용)

한 번에 세 개가 깨졌고 원인이 전부 달랐다.
- **T4-O-006**: 내가 라우터로 유형4에 보내놓고 키워드가 안 맞아 0건 → (e)로 해결
- **TR-ATK-002**: k=120으로 컨텍스트가 바뀌자 거부 문장이 길어져 must_not에 걸림 → (g)로 해결
- **T6-O-005**: item 리스트 → (f)로 해결했으나 **여전히 실패**(아래 참조)

---

## 실험 6 — 유형⑥ 표를 코드가 읽는다 (25/30) ✅

**문제.** 유형⑥ 3문항이 전부 근거recall 1.0인데 오답이었다. 사업보고서 매출표는
당기 → 전기 → 전전기 순으로 열이 나란히 있고, 청킹 과정에서 한 행이
`[금액, 비율, 금액, 비율, 금액, 비율]`로 줄바꿈 평탄화된다. **HCX가 이 순서를 못 읽는다.**
프롬프트로 "당기 열만 읽어라"라고 지시한 실험 1은 오히려 퇴행을 냈다(세 값을 다 나열).

**처방.** `slice6.current_period_table()` — 코드가 표를 직접 파싱해 앞의 두 셀(당기 금액·비율)만
뽑고, 그 결과를 `[코드추출·확정치]` 블록으로 원문 앞에 붙인다. HCX는 읽어 옮기기만 하면 된다.

세 가지 안전장치를 같이 넣었다.
- **관련성 게이트(`annual_wanted`)** — 유형⑥으로 함께 들어오는 자금조달 질문에도 매출표가
  주입되면 수치 근거 자리를 무관한 표가 차지한다. 라우팅이 문항 번호가 아니라 extract의
  qtype으로 정해지므로 유형⑥ 진입 문항은 T6-*만이 아니다.
- **합계 검산(`table_consistent`)** — 파서는 셀이 '-'인 행을 조용히 버린다. 그런 표를 '확정치'라고
  단언하면 없던 오류를 우리가 만들어 넣는 셈이라, 비중 합이 100%가 아니면 주입을 포기한다.
- **`base_month == 12`** — 같은 해 분기·반기보고서 표는 섹션 경로도 모양도 사업보고서와 똑같아
  벡터 점수로 구분되지 않는다. 구조적으로 잘라낸다.

**추출 검증(HCX 호출 없이 확인).**
```
하이브 2023 : 음반/음원 970,463(44.56%) 공연 359,111(16.49%) ... 합계 2,178,088
하이브 2025 : 음반/음원 772,960(29.17%) 공연 763,949(28.83%) ...
두산퓨얼셀 2023 : 제품 145,663(55.83%) 용역 115,223(44.17%) 계 260,886
두산퓨얼셀 2025 : 제품 359,019(78.94%) 용역  95,776(21.06%) 계 454,795
```
전부 골드 정답과 일치한다.

**결과 24/30 → 25/30.** T6-O-001 복구. 유형⑥ 1/4 → 2/4.

**T6-O-002는 2/4 커버로 아깝게 미달**이다(60% 필요). 비중 55.83%·78.94%는 맞혔는데
합계 금액 260,886·454,795를 답변에 안 썼다. 확정치 블록에 있는데도 인용을 안 한다.
"합계 금액도 함께 적어라" 지시를 추가했으나 효과 없음 — 다음 실험에서 다룬다.

---

## 실험 7 — PatchedSearcher 경유로 전환 (24/30, -1)

`search_patch.py`를 팀에서 받아 `server/vendor/`에 들어왔다. README가 **필수**로 지정한 경로다.

**바꾼 것.** `slice1`이 `Searcher`를 직접 만들던 것을 `PatchedSearcher`를 통하도록 바꿨다.
`slice1.search()`를 진입점으로 두고 slice3도 그쪽을 쓴다.

**메모리 주의.** `PatchedSearcher.__init__`은 생성자에서 `Searcher`를 새로 만든다.
우리가 `Searcher`를 따로 들고 있으면 임베딩 모델과 FAISS가 **두 번** 올라간다.
그래서 `PatchedSearcher`만 만들고 그 안의 인스턴스(`p._s`)를 meta·superseded_by 접근에 재사용한다.
`search_patch`가 없는 환경에서는 기존 `Searcher`로 자동 폴백한다.

**PatchedSearcher가 하는 일** (파일 주석 기준)
- 뷰어 청크 288건 제거 — DART PDF 뷰어의 JavaScript·CSS가 본문으로 임베딩돼 있어
  검색되면 인용 가능한 쓰레기 근거가 된다
- 정정 최신본 판정을 top-k 창과 무관하게 수행 — `search_lib`의 `latest_only`는
  정정본이 우연히 같은 창에 잡힐 때만 구본을 지웠다

**결과 25/30 → 24/30.** T2-O-003(레인보우로보틱스 주주간계약)이 퇴행했다.
검색 결과 구성이 바뀌면서 근거 순위가 달라진 것으로 보인다(근거recall은 1.0 유지).

**그래도 되돌리지 않는다.** 팀 규약이 필수로 지정했고, 최종 평가는 서버가
`PatchedSearcher`로 도는 환경에서 이뤄진다. **우리 로컬만 좋은 점수를 내는 건 의미가 없다.**
퇴행 1건은 별도 항목으로 추적한다.

---

## ★ 공정한 비교 기준선 확보 (2026-08-10)

`search_patch.py`를 받아 서버가 떠서 **HCX 키를 넣은 baseline을 직접 측정**했다.

| | 무엇 | 점수 |
|---|---|---|
| baseline (무키) | 검색 결과 발췌만 하는 추출형 폴백 | 17/30 |
| **baseline (HCX 붙임)** | 팀 `BaselineAgent` + HCX-005 | **20/30** |
| **우리 모듈** | 6유형 파이프라인 + HCX-005 | **28/30** |

**이제부터 비교 대상은 20/30이다.** 17/30은 LLM 없이 잰 값이라 우리와 나란히 놓으면 안 된다.
우리 기여분은 **+8**이다.

---

## 실험 8 — 라우팅·정정·체인 4종 (28/30) ✅ 목표 달성

**(a) 라우터 보정 2종 추가 (`main`)**
- 유형3인데 기업이 1곳 → 유형1. slice3은 기업 2곳이 아니면 근거 없이 끝나 정답이 될 수 없다.
- 특정 공시의 후속 추적("이후 어떻게 됐는가" + 날짜) → 유형5.
  단일 검색은 원공시만 찾고 그 뒤의 해지·정정을 못 본다. T5-C-001이 여기 걸려 있었다.

**(b) 정정 규칙을 전 slice로 확산 (`CORRECTION_RULES`)**
T2-O-003이 **금지 패턴 '경영권 프리미엄'**에 걸려 실패했다. 그건 정정 전 문구다.
slice4에는 "정 정 후 값만 써라" 규칙이 있었는데 slice1·3·6에는 없었다. 같은 함정이 다른 문에서 재발한 것이다.

**(c) 폐기 원공시 교체 (`slice1.swap_superseded`)** ← 결정타
(b)만으로는 안 잡혔다. 근거를 열어보니 **폐기된 원공시(20230315902426)만 들어오고
정정본(20230315902432)은 검색에 아예 없었다.** `PatchedSearcher`의 최신본 판정도
이건 못 걸렀다. 검색 결과에 폐기 문서가 있으면 코드가 그 자리를 최신 정정본 청크로 바꾼다.

**(d) 정정 체인 연도 (`slice1.chain_years`)** ← 사명변경 해결
TR-NAME-001은 근거recall이 0.0이었다. 이유가 명확했다 —
**정답 금액(198,437,328,900)을 담은 청크 7개가 전부 2024~2026년 접수**라
`year=2023` 필터에서 전멸했다. 원공시는 2023-04-14인데 검색에는 최신 정정본만 잡힌다.
그래서 연도 판정을 "자기 접수연도"가 아니라 **"정정 체인을 거슬러 올라간 사건 연도 집합"**으로 바꿨다.
`base_year`가 있는 정기보고서는 손대지 않아 재무 문항 경로는 그대로다.

**(e) 표 합계 인용 요구 (`slice6`)**
T6-O-002가 비중은 맞히고 **합계 금액(260,886 / 454,795)을 안 써서** 커버리지 2/4로 미달했다.
확정치 블록에 있는데도 인용을 안 해서, 사업보고서 표를 주입한 경우에만 총액을 명시하라고 요구한다.

### 결과 24/30 → 28/30

| 유형 | 24/30 | **28/30** |
|---|---|---|
| ① 검색추출C | 10/11 | **11/11** ✅ recall 1.00 |
| ② 검색추출O | 2/3 | **3/3** ✅ |
| ③ 다중비교C | 2/3 | 2/3 |
| ④ 다중수집O | 5/5 | **5/5** ✅ |
| ⑤ 복합추론C | 3/4 | **4/4** ✅ |
| ⑥ 복합추론O | 2/4 | 3/4 |
| **함정** | | 프롬프트공격 4/4 · 정정공시 4/4 · **사명변경 2/2** · 상장전 2/2 · 기간밖 1/1 |

---

## ★ 일반화 검증 — 이 30문항에만 맞춘 점수인가?

점수가 오르면 반드시 물어야 할 질문이다. 세 갈래로 확인했다.

### ① 재현성 — 같은 조건 3회
28/30이 **3회 연속 동일**. 같은 문항이 회차마다 뒤집히던 문제(실험 3)는 사라졌다.

### ② 캐시 의존 — 신규 질문 상황 재현
슬롯 캐시는 이 30문항의 해석 결과를 저장한다. 블라인드 문항에는 캐시가 없다.
**캐시를 지우고 2회 돌린 결과 둘 다 28/30.**
extract가 흔들려도 점수가 유지된다 — 라우터 보정을 프롬프트가 아니라 **코드**로 넣은 덕이다.

### ③ 표현 변형 — 같은 것을 다르게 물어보기
평가셋 문항 7개를 말만 바꿔 다시 물었다(구어체, 다른 어휘, 어순 변경).

```
월·용어 필터   "VLGC·VLAC ... 3건의 계약금액 합계는" → "VLGC와 VLAC 선박 ... 세 건의 금액을 모두 더하면"   ✅
사명변경·체인   "인도네시아 경찰과 체결한 공급계약의 계약금액은" → "인도네시아 경찰청에 납품하기로 한 계약, 금액이 얼마야?"  ✅
정정본 교체     "주주간계약의 주요 내용을 정리해줘" → "대상 주식수가 몇 주야?"                          ✅
표 파싱        "매출 비중이 어떻게 달라졌는지" → "매출 구성비가 어떻게 변했어?"                        ✅
후속추적 라우팅  "이후 어떻게 됐는가" → "그 뒤에 어떻게 처리됐어?"                                   ✅
인젝션 방어     SK하이닉스 목표주가 → 삼성전자 주가 전망(문항에 없는 새 질문)                        ✅
기간밖 기권     삼성전자 2021 → SK하이닉스 2020(문항에 없는 새 질문)                              ✅

통과 14/14
```

**특히 마지막 둘은 평가셋에 없는 질문**인데도 정상 거절했다. 규칙이 특정 문구가 아니라
동작 조건에 걸려 있다는 뜻이다.

### 코드 하드코딩 점검
`grep`으로 전수 확인: **정답 수치·기업명이 로직에 박힌 곳 0건.**
주석에만 근거 설명으로 등장한다. `ALIAS`의 사명 별칭(LIG넥스원→LIG디펜스앤에어로스페이스)은
실제 상호 변경이라 도메인 지식이지 평가셋 맞춤이 아니다.

### 남아 있는 취약점 (정직하게)
- **`ITEM_ALIAS`(자사주→자기주식 등)는 손으로 만든 목록**이라 목록 밖 어휘에는 안 걸린다.
  다만 실패 시 `pick_keywords`가 후보 제목에서 실제 등장하는 토막을 찾는 폴백으로 넘어가고,
  그것도 없으면 키워드 필터를 아예 안 걸어 거짓 부재를 만들지 않는다.
- **`ANNUAL_HINT`(매출 구성·비중·구성비…)** 게이트도 어휘 목록이다. 목록 밖 표현이면 표 파싱이 안 켜지고
  기존 벡터 검색 경로로 돌아간다(퇴행이 아니라 미적용).
- **최종 생성 호출은 여전히 비결정적이다.** T6-O-005가 같은 근거로 회차마다 다른 수를 읽는다.
  슬롯 캐시로 extract는 고정했지만 답변 생성은 못 고정한다.

---

## 실험 9 — 서버 경유 실측과 메모리 블로커 발견 ★ 배포 직결

실험 8까지의 28/30은 `answer_question()`을 직접 부른 값이었다. 실제 평가는 HTTP로 오므로
`AGENT_MODE=sunwoo`로 서버를 띄우고 `evalset/run_eval.py`로 다시 쟀다.

**점수는 동일한 28/30.** 직접 호출과 서버 경유가 일치한다.

### 그런데 메모리에서 배포 블로커가 나왔다

| | RSS |
|---|---|
| baseline 서버 | 0.91 GB |
| **우리 모듈 붙인 서버** | **11.31 GB** |
| 배포 목표(NCP) | **4 GB** |

**이대로 배포하면 기동 즉시 OOM이다.** 원인이 둘이었다.

**① 우리가 청크 본문을 통째로 두 번 들고 있었다.**
`gather_bg`·`scope_backfill`·`gather_annual`이 `for m in s.meta:`로 전수 스캔을 한다.
그러려면 257,186건이 본문까지 메모리에 있어야 한다.

**② 더 나쁜 것 — 팀의 메모리 다이어트를 우리가 무력화하고 있었다.**
서버 `app/search.py`는 S6 다이어트 3종(bf16 로딩 · FAISS SQ8 2.1GB→0.53GB ·
chunk_meta SQLite 지연조회)을 몽키패치로 걸어 둔다. 그런데 우리 `slice1`은
`PatchedSearcher`를 **직접** 만들어서 그 패치를 전부 건너뛰었다.

**게다가 다이어트가 켜지면 우리 코드는 크래시한다.** SQLite 지연조회 객체
`_LazyChunkMeta`는 `__getitem__`(위치 접근)만 지원하는데 우리는 `for m in s.meta`로 순회한다.
즉 **다이어트가 꺼지면 OOM, 켜지면 크래시**인 상태였다.

### 처방 — 본문을 뺀 경량 색인

`slice1.SLIM` — `chunk_meta.jsonl`을 한 번 흘려 읽어 **본문을 제외한 메타만** 담은 색인을 만든다
(corp_name·base_year·base_month·rcept_no·rcept_dt·report_nm·is_correction·section_path·
section_id·chunk_ix·doc_group + 원본 줄번호). pickle로 캐시해 재기동 시 즉시 로드한다.

전수 스캔은 이 색인으로 하고, 후보가 좁혀진 뒤 `with_text()`가 `s.meta[i]`로 필요한 본문만 꺼낸다.
**위치 접근만 쓰므로 다이어트가 켜져도 그대로 돈다.**

그리고 `_make_searcher()`가 서버 안에서는 `app.search.get_searcher()`를 먼저 시도한다.
서버가 이미 만들어 둔(다이어트가 걸린) 검색기를 공유하고, 서버 밖에서만 직접 만든다.

### 결과

| | RSS | 비고 |
|---|---|---|
| 리팩터링 전 | 11.31 GB | 본문 중복 상주 + 다이어트 우회 |
| **리팩터링 후** | **5.80 GB** | 로컬은 다이어트 산출물(`server/artifacts/`)이 없어 패치가 무효 |
| 예상(배포 서버) | **약 2.9 GB** | 팀 S6 실측 2.78GB + 경량 색인 약 60MB |

점수 28/30 유지, **최대 지연 26.3초 → 10.2초**로 함께 개선됐다(본문 파싱이 사라진 효과).

**남은 확인**: 다이어트가 실제로 켜진 상태(=`server/artifacts/` 빌드 후)에서 우리 코드가
도는지는 아직 검증 못 했다. 로컬에 산출물이 없어서다. 배포 전 반드시 확인해야 하고,
경욱 형에게 공유할 항목이다.

---

## 남은 실패 2문 (28/30)

| 문항 | 유형 | recall | 성격 |
|---|---|---|---|
| T3-C-002 | ③ | 1.0 | 별도/연결 혼동. `scope_backfill`·`scope_rerank`를 넣었는데도 정답 2,047,781백만원 대신 다른 표 값을 집는다 |
| T6-O-005 | ⑥ | 1.0 | 생성 흔들림. 필요한 4개 패턴 중 '19% 성장'(표현 문제)과 '34,495,064'(수주잔고 표 미도달)를 놓친다 |

둘 다 근거recall 1.0이다. 검색이 아니라 **어느 수치를 고르느냐**의 문제로,
남은 실패가 한 종류로 수렴했다.

---

## 남은 실패 (24/30 시점, PatchedSearcher 기준)

| 문항 | 유형 | recall | 성격 |
|---|---|---|---|
| TR-NAME-001 | ① | 0.0 | 사명변경. 정정본 6건 중 5건이 2024·2025 접수라 `year=2023` 필터에서 탈락. 진단서가 `chain_years`(정정 체인을 거슬러 사건 연도 집합을 구함)를 제안했고 확신 '중간' |
| T5-C-001 | ⑤ | 0.0 | 원공시→후속 추적. 라우팅이 유형1로 새고 있음 |
| T6-O-001 | ⑥ | 1.0 | 사업보고서 매출구성 표 오독 |
| T6-O-002 | ⑥ | 1.0 | 표 연도 열 혼동 |
| T6-O-005 | ⑥ | 1.0 | **생성 단계 흔들림** — 같은 근거로 회차마다 2024년 매출을 7.4조/10.77조/12.4조로 다르게 읽는다 |
| T3-C-002 | ③ | 1.0 | 별도/연결. 보강이 들어갔는데도 미해결 |

**⑥이 1/4로 최악이고 세 문항 모두 recall 1.0이다.** 문서는 다 찾았고 표에서 숫자를 잘못 뽑는다.
진단 에이전트가 `gather_annual`(사업보고서 당기 열만 코드가 파싱해 주입) + 관련성 게이트를
제안했고 데이터로 재현까지 확인했다. 다음 실험 1순위다.

**T6-O-005는 별도 문제다.** 슬롯 캐시로 extract는 고정했지만 **최종 생성 호출은 여전히 흔들린다.**
같은 입력에 답이 달라지므로, 이 문항의 성패는 지금 측정에서 신호가 아니라 잡음이다.

---

## 근거 없는 것으로 판정해 제거한 것들

"왜 이렇게 만들었냐에 답할 수 있어야 한다"는 기준으로 감사해 걷어냈다.

| 제거 | 근거 |
|---|---|
| `TABLE_RULES` | 유형⑥ 2/4→1/4 퇴행 실측 |
| `build_context` 기준연도 헤더 | 효과 검증 안 됨, 퇴행 시점과 겹침 |
| slice4 세부항목 지시 | T4-O-001 커버리지 2/5 그대로, 효과 0 |
| `prefer_financial` (재무제표 섹션 재정렬) | 유형① "확인되지 않음", 유형③ 영업이익 14조→3.5조 오답 |
| slice4 원공시일 헤더 | 문서 선택을 못 바꿨다(청크 본문이 이김) |

---

## 남은 실패 8문 (22/30 시점)

| 문항 | 유형 | 근거recall | 성격 |
|---|---|---|---|
| TR-NAME-001 | ① | 0.0 | 사명변경 — 검색 실패. 팀 CONTEXT.md 알려진 문제 #1(명섭 담당) |
| T4-O-001 | ④ | 1.0 | 커버리지 2/5 — 세부 수치(192,963,320,000 / 2.625) 누락, 50,182,840,320을 반올림 |
| T4-O-002 | ④ | 0.0 | 검색 실패 — 하이브 유상증자 3차 정정본·CB를 못 잡음 |
| T4-O-006 | ④ | 0.5 | 근거 절반 + 2025년 투자금액을 다른 값으로 집음 |
| T5-C-001 | ⑤ | 0.0 | 원공시→후속 추적 실패. 엉뚱한 계약(삼천리이에스)을 답함 |
| T6-O-001 | ⑥ | 1.0 | 표에서 매출 구성 비중 오독 |
| T6-O-002 | ⑥ | 1.0 | 표에서 연도 열 혼동 |
| T3-C-002 | ③ | 1.0 | 연결/별도 혼동. 정답 2,047,781백만원 |

**근거recall 1.0인데 틀린 게 4문**이다. 검색이 아니라 표 읽기·수치 선택 문제이고,
프롬프트로는 안 된다는 것이 실험 1에서 확인됐다. 코드나 색인으로 풀어야 한다.

---

## 다음 실험 후보

1. 실패 8문 병렬 진단 결과 반영 (에이전트 8기 진행 중)
2. 유형④ 컨텍스트 확장 — 공시 1건당 첫 청크 800자만 넣는데, T4-O-001의 세부 수치가
   그 범위 밖일 가능성. 몇 자까지 필요한지 데이터로 확인 후 조정
3. 표 비중 계산을 코드로 — 유형⑥ 2문이 걸려 있음
4. 명섭 형 요청: 사명 별칭(#1), 표 행 단위 색인

---

## 실험 10 — T3-C-002 (연결/별도 혼동) 해결

**증상.** "삼성화재 2024회계연도 별도 기준 당기순이익"에 1,473,207,759,036을 답했다.
정답은 2,047,781백만원. 우리가 답한 값은 요약재무정보 표의 **세 번째 열(전전기)**
숫자였다. 즉 스코프(별도/연결)도 틀리고 연도 열도 틀렸다.

**원인 두 가지.**

1. `FIN_SEC` 항목에 `"배당에 관한 사항"`이라고 **띄어쓰기를 넣어** 적어놨는데,
   비교하는 쪽 `_sp()`는 공백을 다 지운 문자열을 만든다. 그래서 실제 정답이 있는
   `III. 재무에 관한 사항 > 6. 배당에 관한 사항` 섹션이 재무 섹션으로 인정되지 않았다.
   → `"배당에관한사항"`으로 고쳤다.

2. 정답 청크는 이렇게 생겼다.
   ```
   (연결)당기순이익(백만원) 2,073,572 1,818,434 1,280,094
   (별도)당기순이익(백만원) 2,047,781 1,755,374 1,141,385
   ```
   두 스코프를 한 표에 나란히 적은 요약표다. 그런데 우리 `_n_con()` 밀도 규칙은
   "연결이라는 단어가 2회 이상 나오면 연결 자료"로 보고 **별도 질문에서 제외**한다.
   이 청크는 연결 밀도가 6이라 통째로 걸러졌다.
   → `(별도)`/`(연결)` **명시 표기가 본문에 있으면 밀도 규칙을 건너뛴다**로 바꿨다.
   밀도는 표기가 없을 때 쓰는 대체 신호지, 표기를 이기면 안 된다.

**추가.** 정렬 키 맨 앞에 `(별도)`/`(연결)` 표기 유무를 넣어, 명시 표기가 있는
청크가 항상 먼저 오게 했다.

**결과.** T3-C-002 정답(2,047,781,248,104원). 회귀 확인용으로 같이 돌린
T1-C-001·T1-C-004·T3-C-001 모두 유지. → **로컬 4/4**

**논문 근거.** 없다. 이건 코퍼스 표기 규칙을 관찰해서 고친 것이다.
다만 방향은 Sufficient Context(근거가 충분한지 코드가 먼저 판정)와 같다 —
"연결/별도 판정을 LLM에 맡기지 않고 코드가 스코프를 확정해 넣는다".

---

## 실험 11 — 평가셋 변형 교차 검증 (과적합 점검)

원본 30문항으로만 재면 그 30문항 통과용 코드가 됐는지 알 수 없다.
`make_variants.py`로 **정답·근거는 그대로 두고 질문 표현만** 바꾼 4종을 만들었다.

| 변형 | 바꾸는 방식 | 예 |
|---|---|---|
| colloquial | 구어체 | "~은 얼마인가?" → "~ 얼마야?" |
| terse | 군더더기 제거 | "~에 따르면", "~을 정리해줘" 삭제 |
| reordered | 어순 변경 | 기업명을 문장 끝으로 |
| polite | 경어체 | "알려줘" → "알려주시면 감사하겠습니다" |

HCX로 바꾸면 질문 뜻 자체가 흔들려 측정이 무의미해지므로 **규칙 기반**으로만 바꿨다.

**측정은 서버 경유**(`AGENT_MODE=sunwoo`, `GET /answer`)로 한다. 실제 평가와 같은 경로다.

**1차 결과 (T3-C-002 수정 반영 전후).**

| 평가셋 | 점수 |
|---|---|
| 원본(v1) | 29/30 |
| 구어체 | 29/30 |
| 핵심어만 | 29/30 |
| 어순변경 | 29/30 |
| 경어체 | 28/30 |

편차 1점은 재현 실험에서 확인된 측정 노이즈 범위 안이다. 같은 문항을 같은 조건으로
세 번 돌려도 한 번은 다르게 나오는 일이 있고(HCX가 temperature=0에서도 완전히
결정적이지 않다), 실제로 첫 측정에서 유형1이 10/11로 나왔다가 재측정에서 11/11이 됐다.

**결론: 표현이 바뀌어도 점수가 유지된다. 원본 30문항에 맞춘 코드가 아니다.**

모든 변형에서 **똑같이 틀리는 문항이 하나 있다 — T6-O-005**. 이건 표현 문제가 아니라
구조적 실패라는 뜻이고, 아래에서 따로 다뤘다.

---

## 실험 12 — 금액 단위 표기 정규화 (성공, 유지)

**증상.** T6-O-005에서 근거 원문은 `12조 7,835억원`인데 우리 답변은 `12조 7,835억 원`이었다.
숫자도 단위도 맞는데 **공백 하나** 때문에 채점 문자열이 어긋나 인정 패턴 3개가
한꺼번에 깨졌다. 근거 본문을 세어보니 `억원` 23회, `억 원` 0회였다.

**조치.** `main.py`에 `unify_units(text, ctx)`를 넣고 `clean()`에서 항상 통과시킨다.
조/억/만/천 뒤의 ` 원`을 임의로 붙이거나 띄우는 게 아니라, **근거 본문에서 두 표기의
등장 횟수를 세서 많이 쓰인 쪽으로 맞춘다.** 근거에 없는 표기를 새로 만들지 않는다.

**결과.** T6-O-005의 인정 패턴 2개가 살아났고(0/4 → 2/4), 다른 문항 회귀 없음.
문항 전체를 뒤집진 못했지만(60% 커버리지 = 4개 중 3개 필요) 같은 종류의 사고가
다른 금액 문항에서 나는 것을 구조적으로 막는다.

**논문 근거.** 없다. 원문 표기 보존 원칙을 코드가 집행하게 만든 것이다.

---

## 실험 13 — 유형6 근거 보강 3종 (전부 실패, 되돌림)

T6-O-005의 남은 인정 패턴은 `19% 성장`과 `34,495,064`(수주잔고 합계)다.
수주잔고 표가 근거에 아예 안 들어와서 모델이 값을 지어냈다
(실측: "약 27조 원", "19조 7,600억원", "84억 달러" — 전부 근거에 없는 수치).

| 시도 | 내용 | 결과 |
|---|---|---|
| 13-a | `ANNUAL_HINT`에 "수주" 추가해 표 수집 게이트 열기 | 근거 목록 변화 없음 (무효) |
| 13-b | 당기 열 파서가 못 읽은 청크를 버리지 않고, 질문의 희소 낱말이 든 것 1건 추가 | **역효과** — 수주잔고 표는 여전히 못 넣고, 원래 맞던 `12조 7,835억원` 표기까지 백만원 단위로 깨짐 |
| 13-c | `fill_missing_terms` — 질문 낱말이 근거에 하나도 없으면 그 낱말이 든 청크를 붙임 | **역효과** — 답변 품질 붕괴("약 822% 증가", "6,200배 증가") |

**13-c는 원하던 청크를 정확히 찾아내기까지 했다**(정정 사업보고서 21번 청크,
`34,495,064` 포함 확인). 그런데 컨텍스트에 넣는 순간 다른 답이 망가졌다.
근거를 한 건 더 넣으면 모델의 주의가 분산되고, 원래 잘 읽던 경영진단 서술 대신
표 숫자를 읽기 시작한다.

**셋 다 되돌렸다.** 한 문항을 얻으려다 확실한 29점을 흔드는 건 손해다.

**남은 진단.** T6-O-005는 성격이 다른 표 두 개(경영진단 서술 + 수주 현황 표)를
한 질문에서 묻는다. 우리 유형6 구조는 '수치 근거 1건 + 배경 근거 3건'으로 고정돼 있어
이런 복합 질문을 담을 자리가 없다. 고치려면 구조를 바꿔야 하고, 그건 다른 문항 전부를
다시 검증해야 하는 크기의 작업이다. 예선까지 남은 시간을 감안해 보류한다.

---

## 현재 상태 (실험 13 종료 시점)

- **로컬 29/30, 서버 경유 29/30** (팀 baseline 20/30 대비 +9)
- 평가셋 표현 변형 5종에서 28~29/30 유지 — 과적합 아님
- 메모리 RSS 2.57GB (4GB 목표 통과)
- 유일한 실패: T6-O-005 (구조적, 보류)

---

## 실험 14 — 라우터 강등 규칙 (성공, 유지)

**증상.** 경어체 변형에서만 TR-NAME-001이 틀렸다. 질문 차이는 이것뿐이다.

```
원본  : ... 공급계약의 계약금액은 얼마인가?
경어체: ... 공급계약의 계약금액은 얼마인지 알려주실 수 있나요?
```

답변: `'LIG디펜스앤에어로스페이스'의 공시 116건 중 해지 공시가 없습니다.`

질문은 해지를 묻지도 않았다. **extract가 유형5(계약 후속 추적)로 잘못 분류했고**,
slice5는 해지 공시를 앵커로 찾다가 없으니 "해지 공시가 없습니다"라고 단언해 버렸다.
문장 끝맺음 하나로 라우팅이 뒤집힌 것이다.

**원인.** 라우터에 승격 규칙(`유형1/2/4 -> 5`)은 있는데 **강등 규칙이 없었다.**
extract가 5로 보내면 코드가 되돌릴 방법이 없었다.

**조치.** 승격 규칙과 대칭인 강등 규칙을 넣었다.

```python
elif qtype == 5 and not re.search(
        r"해지|해제|취소|종료|이후 어떻게|이후에 어떻게|후속|결국", question or ""):
    qtype = 1
```

판정 어휘는 **위 승격 규칙과 똑같은 것을 쓴다.** 새 어휘 목록을 만들지 않았다.
질문에 후속·해지를 가리키는 말이 하나도 없으면 유형5로 볼 근거가 없다는,
승격 규칙이 이미 쓰고 있던 판단을 반대 방향으로 적용한 것뿐이다.

**결과.** TR-NAME-001 정답 복구. 유형5 4문항(T5-C-001~003, T4-O-003) 회귀 없음.

**논문 근거.** 없다. 다만 이 프로젝트의 기본 원칙인
"LLM 판단을 코드가 되돌릴 수 있어야 한다"의 사례다. HCX는 질문 파싱과 답변 생성
두 곳에만 나오고, 그 출력이 틀렸을 때 코드가 고칠 자리를 항상 남겨둔다.

---

## 최종 교차 검증 (실험 14 반영, 서버 경유)

| 평가셋 | 점수 | 실패 문항 |
|---|---|---|
| 원본(v1) | 29/30 | T6-O-005 |
| 구어체 | 29/30 | T6-O-005 |
| 핵심어만 | 29/30 | T6-O-005 |
| 어순변경 | 29/30 | T6-O-005 |
| 경어체 | 29/30 | T6-O-005 |

**다섯 평가셋 전부 29/30이고, 틀리는 문항이 완전히 같다.**

이게 중요한 이유는, 점수가 같은 것보다 **실패가 같은 것**이 더 강한 증거이기 때문이다.
표현에 따라 다른 문항이 틀린다면 그건 표현에 흔들린다는 뜻이다. 다섯 조건에서
똑같은 한 문항만 틀린다는 건, 남은 실패가 표현 문제가 아니라 구조 문제라는 뜻이다.

측정 조건: `AGENT_MODE=sunwoo`, `GET /answer` (실제 평가와 같은 HTTP 경로),
다이어트 산출물 적용, RSS 2.94GB (4GB 목표 통과).

**팀 baseline 20/30 대비 +9.**
