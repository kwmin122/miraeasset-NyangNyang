# .env 로 복사해서 사용 (git에 올라가지 않음)
AGENT_MODE=sunwoo            # mock | baseline | sunwoo
# ★ sunwoo 가 제출 모드다. mock 은 더미 응답이라 그대로 띄우면 전 문항 0점이 된다.
CLOVA_API_KEY=             # CLOVA Studio에서 발급 (최초 1회만 표시됨!)
HCX_MODEL=HCX-005
